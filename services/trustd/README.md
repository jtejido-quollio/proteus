# Trustd Service (Core)

Proteus core API for record/verify and transparency log operations.
Entrypoint: `services/trustd/cmd/trustd`.
Shared Go libraries live under `packages/shared-go/` and `internal/` for now.
